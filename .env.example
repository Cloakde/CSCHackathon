# Server-only provider credentials. Never prefix these with NEXT_PUBLIC_.
ELEVENLABS_API_KEY=
GENERATION_API_KEY=

# Public application origin used for explicit extension-to-web handoff.
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000
