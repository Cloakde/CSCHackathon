# TASK-101/102 — Codex review and correction

**Status:** correction IN REVIEW, 2026-09-07. No self-approval, TASK-101/102 PASS, TASK-307 integration, or main promotion.

The user requested “Okay fix everything” after independent review of Claude's combined submission at `833651634364f98ec8241185f7a16c61eda542c5`. That source passed 480 existing tests and CI [34170592864](https://github.com/Cloakde/CSCHackathon/actions/runs/34170592864), but additional offline probes exposed substantive failures. Those passing checks were insufficient evidence of correctness.

## Findings and corrections

| Finding                                                                               | Corrected behavior and regression evidence                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Wrong provider message field, omitted outgoing message kind                           | Parse documented `message_type`; send `message_type: "input_audio_chunk"`; handle documented spacing tokens and permanent terms errors. Fake frames follow the public API examples.                                                                                                                                                                                    |
| Default UUIDs rejected by canonical ID validation                                     | Generate schema-compatible IDs. The bundled transport is exercised through the actual smoke runner without an injected ID factory, including committed transcript events.                                                                                                                                                                                              |
| Reconnect could compress the lecture timeline                                         | Each socket starts from its first actually transmitted absolute sample. Tests include initial dropped samples, a disconnect gap, and a later committed passage. Nothing is replayed or shifted back to the previous commit.                                                                                                                                            |
| Partials and discarded gaps retained an already committed start                       | Advance segment bounds after every commit; only the remaining uncommitted interval is reported as discarded.                                                                                                                                                                                                                                                           |
| Delayed timestamp responses overwrote pending commits; duplicate timing caused errors | Bounded ordered pending list, exact text pairing, five-second timestamp deadline, duplicate identity check before monotonicity rejection. Ambiguous or missing evidence terminates visibly.                                                                                                                                                                            |
| CONNECTING sends and old socket callbacks corrupted recovery                          | Require socket open plus session start; discard unavailable input with a gap report. Detach/invalidate old connections, cancel pending token work, and ignore late callbacks after Stop.                                                                                                                                                                               |
| Panel opening was delayed behind the state queue                                      | Invoke the gesture-sensitive API directly in the toolbar callback. A regression blocks reconciliation and still observes immediate panel opening. Real Chrome behavior remains a manual gate.                                                                                                                                                                          |
| Navigation/switching preserved consent; late acquisition survived Stop                | Invalidate pending consent and stream requests on tab changes/close or Stop. Offscreen operation generations stop late tracks and prevent duplicate acquisition.                                                                                                                                                                                                       |
| Missing outside-panel recording indication and misleading simulation text             | REC badge and toolbar title reflect starting/active capture. Cancel is available before capture completes. A capture spike's panel explicitly separates synthetic transcript content from captured audio.                                                                                                                                                              |
| “Inert” capture was reachable in ordinary builds                                      | Production background defaults off unless explicitly built with the capture-spike flag. Source tests and the built-worker package check exercise clicks and consent and verify zero capture attempts.                                                                                                                                                                  |
| Request limits were enforced only after reading complete bodies                       | Incremental byte limits and abortable deadlines cover both headers and bodies. Tests cover oversized incoming streams and stalled incoming/upstream bodies.                                                                                                                                                                                                            |
| Deferred manual launcher could not establish trustworthy evidence                     | Correct Windows Node/Next entry points, bundle the exact transport/schema sources, isolate child environments, authenticate readiness without minting, verify the listener's process/address, require synthetic speech and forced reconnect, and clean children on failure/interruption. Offline tests exercise the real bundled adapter with fake network and clocks. |

Changed tests are in `extension/test/App.test.tsx`, `extension/test/{background,capture-controller,offscreen,scribe-transport}.test.ts`, `web/src/lib/server/scribe/*.test.ts`, and `scripts/manual/scribe-spike.test.mjs`. Existing budget, consent, resampler, simulation and assistance checks remain in the normal suite.

## Verification

Application correction: `057d79eed30a4571372e3031417fc1800d97c315`. The accompanying checkpoint changes documentation only; review the exact final shared head.

`npm run check` passed formatting, lint, secret scanning, type checking, **516 tests (17 root + 73 shared + 285 web + 141 extension)**, all production builds, packaged-worker verification and the production HTTP demo. The final offscreen race passed its targeted 10-test suite as well. The separate guarded MeltingPot component journey passed 1/1 and left the copy clean and unchanged. These are offline engineering results, not manual acceptance. Final CI is visible in [PR #5 checks](https://github.com/Cloakde/CSCHackathon/pull/5/checks); match its head SHA.

Reproduce offline with:

```text
npm run check
npm run test:meltingpot -- --meltingpot-root=C:/Users/abuiz/Documents/Codex/2026-09-04/MeltingPot-rework
node scripts/manual/run-local-scribe-spike.mjs
```

The last command, without arguments, prints an offline plan only. Normal checks use injected responses or synthetic local services; they never run the paid smoke. This correction was checked with a child environment that did not inherit provider credentials or activation.

The separate MeltingPot component journey must leave the read-only copy clean at `9244a641e0639982d4eece09b2274a05ee355096`. Original MeltingPot repositories and services are outside scope. Main stays at `8cfa83b88c0f6186d3475266b005069da4fbe820`.

## Later manual checks — not executed or authorized here

1. Another user-started AI reviews the exact final shared head, including these corrections. Earlier TASK-304/305/306 review obligations are not implicitly approved.
2. For TASK-101, obtain session permission for the contract's Chrome matrix. Only an explicitly prepared local spike build uses `VITE_LIVELECTURE_CAPTURE_SPIKE=true`; normal packaged verification deliberately requires capture off. This spike captures for passthrough only and does not feed the sample transcript. Confirm intended-tab consent, audible playback, persistent indicators, reopen/Stop, navigation, worker recovery, silence survival, upgrade behavior and local API access.
3. For TASK-102, follow its separate paid activation gate: explicit capped authorization, accepted terms, a restricted local server key, account quota/retention checks, and synthetic speech only. The launcher requires a clean reviewed source tree and `LIVELECTURE_SPIKE_SOURCE_TREE` matching `git rev-parse HEAD^{tree}`; an exact 32-character Chrome extension ID in `LIVELECTURE_EXTENSION_ID`; `SCRIBE_SYNTHETIC_PCM` pointing to mono PCM16LE at 16 kHz, at most 30 seconds in 0.1-second units; `SCRIBE_SYNTHETIC_AUDIO_CONFIRMED=synthetic-speech-only`; and `RUN_PAID_SCRIBE_SMOKE=I_ACKNOWLEDGE_COST`. The bytes can reject silence/misalignment but cannot independently prove the operator's synthetic-speech declaration. Do not set these gates or execute them during offline work.
4. Only after that authorization, `node scripts/manual/run-local-scribe-spike.mjs --execute` builds current sources, starts a new loopback-only server, verifies its PID/address, then runs the bounded harness. Default port is 3100; another valid port can be selected with `LIVELECTURE_SPIKE_PORT`. Root/web local environment files are refused so Next cannot silently load extra credentials. The permanent key reaches only the server, never the transport/harness/extension. A new capability is generated per run and never printed.
5. Record actual cost delta separately, actual word timestamps and reconnect behavior, and account retention evidence. The harness reports no-warning retention as **not independently confirmed**. A silence-only, no-commit or missing-reconnect run fails. Its success alone does not replace all TASK-102 manual evidence.

The harness is capped at 30 seconds of transmitted audio, 90 seconds of transport wall time, two connection attempts, two token issuances and one reconnect. The launcher also bounds readiness/build/child lifetime. It logs event names, sequence/timing, gap ranges, artifact hash and safe outcome metadata; no raw transcript, audio, provider frame, key, capability or token-bearing URL.

TASK-307's audio tap and `LiveTranscriptSource` remain unbuilt. Real audio, Gemini quality, browser behavior and live provider compatibility have not been established by fake-only tests. Simulation is the required working demo.

## Primary references checked on 2026-09-07

- [ElevenLabs realtime API](https://elevenlabs.io/docs/api-reference/speech-to-text/v-1-speech-to-text-realtime): message discriminator, PCM JSON, session/commit/timestamp frames and logging request.
- [ElevenLabs event reference](https://elevenlabs.io/docs/eleven-api/guides/how-to/speech-to-text/realtime/event-reference): named errors including unaccepted terms.
- [ElevenLabs JavaScript Scribe reference](https://elevenlabs.io/docs/eleven-api/resources/libraries/scribe-stt/javascript-scribe): timed word items and spacing tokens. No SDK was added.
- [Chrome capture guide](https://developer.chrome.com/docs/extensions/how-to/web-platform/screen-capture) and [side panel API](https://developer.chrome.com/docs/extensions/reference/api/sidePanel): toolbar invocation and offscreen capture lifecycle. Documentation research is not a human Chrome test.
